#include <stdio.h>
#include <conio.h>

int main()
{
    int n;
    int x, y;
    int pie = 3.14;
    int m = 180;

    printf("entert the value of angle\n");
    scanf("%d", &n);

    y = cos(x);
    x = (pie / m) * n;

    printf("%15f is value of cosine", y);
    return 0;
}